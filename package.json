{
  "name": "universe-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd universe-app && npm install --include=dev && npm run build && cd ../universe-backend && npm install --omit=dev",
    "start": "node universe-backend/src/index.js"
  }
}
